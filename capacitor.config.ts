import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tahdigi.visualizer',
  appName: 'TahaAi Visualizer',
  webDir: 'www'
};

export default config;
