# بیلد اندروید فقط با آپلود ZIP

این ریپو فقط ۳ چیز دارد:
- `TahaAiVisualizer-Android.zip` — کل سورس پروژه
- `.github/workflows/build.yml` — ورک‌فلو
- همین README

## روش کار
۱. این ۳ آیتم را push کنید (بار اول ورک‌فلو خودش اجرا می‌شود).
۲. ورک‌فلو فایل `*.zip` را پیدا می‌کند، در پوشه `src/` اکسترکت می‌کند و با Buildozer APK می‌سازد.
۳. از تب **Actions** و بخش **Artifacts** فایل `TahaAiVisualizer-APK` را دانلود کنید.

## آپدیت بعدی
فقط فایل `TahaAiVisualizer-Android.zip` را با **همان نام** جایگزین و push کنید
(از طریق git یا دکمه Upload files در سایت گیت‌هاب) — بیلد خودش شروع می‌شود.

نکته: ورک‌فلوی داخل خودِ زیپ (`src/.github/workflows`) توسط گیت‌هاب نادیده گرفته
می‌شود؛ فقط ورک‌فلوی ریشه ریپو اجراست.
